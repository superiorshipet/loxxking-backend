[phases.setup]
nixPkgs = ["dotnet-sdk_10"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish src/Api/Api.csproj -c Release -o /app/publish"]

[start]
cmd = "dotnet /app/publish/Api.dll"
